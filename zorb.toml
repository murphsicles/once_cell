[package]
name = "@core/once_cell"
version = "1.21.4"
edition = "2026"
authors = ["Zak Murphy <zak@z-lang.org>"]
description = "Single assignment cells and lazy values — Zeta port, faster than the Rust original"
license = "MIT OR Apache-2.0"
repository = "https://github.com/murphsicles/once_cell"
keywords = ["lazy", "static", "once", "cell"]
categories = ["memory-management", "concurrency"]

[fmt]
style = "zeta-strict"
max_width = 100
use_tabs = false

[lint]
tool = "zippy"
level = "pedantic"
